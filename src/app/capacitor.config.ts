import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.apas',
  appName: 'aapashemein',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
